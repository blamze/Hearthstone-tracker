import template from './decks.html';
import controller from './decks.controller';
import './decks.styl';

let decksComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default decksComponent;
